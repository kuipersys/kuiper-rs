apiVersion: ext.api.cloud-api.dev/v1alpha1
kind: ResourceDefinition
metadata:
  name: sensors
  namespace: global
spec:
  group: iot.cloud-api.dev
  scope: Namespace
  names:
    kind: Sensor
    singular: sensor
    plural: sensors
    shortNames:
      - sn
  versions:
    - name: v1alpha1
      enabled: true
